import {
  everyEach
} from 'params/helpers/every-each';

module('EveryEachHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = everyEach(42);
  ok(result);
});
