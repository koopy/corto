import {
  routeResolver
} from 'params/helpers/current-route';

module('CurrentRouteHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = routeResolver(42);
  ok(result);
});
